console.log("Don't forget to make sure popups are enabled");

// Create a test window that we can resize.  Doesn't cover mutation tests yet
testWindow = window.open("", "reactive-window-tests", "width=100, height=100");
